import { assert, describe, it } from "vitest";
import { httpClient } from "../utils";

describe("DELETE /api/newsletter/unsubscribe", () => {
  it("deve cancelar inscrição com sucesso", async () => {
   /* const response = await httpClient.delete("/api/newsletter/unsubscribe", {
      data: { email: "test@email.com" },
    });

    expect(response.status).toBe(200);
    expect(response.data.message).toBe("Inscrição cancelada");
    */
  });
});
